# Server Configuration
NODE_ENV=development
PORT=5000

# JWT Secret (Change in production!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wms_db
DB_USER=postgres
DB_PASSWORD=postgres

# AWS S3 Configuration (for image uploads)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
S3_BUCKET_NAME=wms-inventory-images

# Email Configuration
EMAIL_FROM=noreply@wms.com

# Mailtrap (for testing)
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=b00cd5ca86383c
SMTP_PASS=4d87e8419e5558

# AWS SES Configuration (for production)
# AWS_SES_REGION=us-east-1

# OR SendGrid (alternative)
# SENDGRID_API_KEY=your-sendgrid-api-key

# OR Gmail
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Cron Job Schedule
LOW_STOCK_CHECK_CRON=0 9 * * *

# Email Recipients for Low Stock Alerts (comma-separated)
LOW_STOCK_ALERT_EMAILS=manager@company.com,admin@company.com
